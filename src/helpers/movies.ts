import {Movie} from 'types';
import {SortType} from 'components/MoviesSection/MoviesSection';

const sortMovies = ({movies, sortType}: {movies: Movie[]; sortType: SortType}) => {
  console.log('a');
};